// Shared content width for module pages. The AppShell wrapper provides the outer
// cap (~1440px); modules use this to fill that space and center their content so
// nothing pins to the left on wide desktops. Form-heavy pages (e.g. Settings) may
// use a narrower centered width for readability.
export const CONTENT_MAX = 1320
export const FORM_MAX = 820
